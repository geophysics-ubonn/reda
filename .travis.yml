sudo: false # Use docker containers rather than VMs -> faster
language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
install:
  - pip install --user -q -r requirements.txt -r doc/requirements_doc.txt
  - pip install --user -q .

script:
  - make test
  - make doc

